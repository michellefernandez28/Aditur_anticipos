model solo representan datos y comportamiento del objeto. no debería saber si existe un archivo, ni cómo se escribe.

dao accede a datos pero no debería preocuparse por infraestructura
//DAO = Data Access Object
//-Intermediario entre el programa y la “base de datos”
//Se usa para:
//leer datos
//guardar datos
//buscar registros
//actualizar
//eliminar

Util sirve para utilidades del código, variables estáticas y manejar archivos
